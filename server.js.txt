const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.send('<h1>Welcome to My E-Commerce App 🚀</h1><p>Hosted on Azure via GitHub Actions</p>');
});

app.listen(port, () => console.log(`Server running on port ${port}`));
